// Re-export auth config for backward compatibility
export { handlers, signIn, signOut, auth } from "./auth-config";

// Re-export registerUser from separate file
export { registerUser } from "./register";
